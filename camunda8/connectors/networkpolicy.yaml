apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: connectors-network-policy
spec:
  podSelector:
    matchLabels:
      app: connectors
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: zeebe-gateway
      ports:
        - protocol: TCP
          port: 8080