apiVersion: v1
kind: Service
metadata:
  name: web-modeler-websockets
  labels:
    app: web-modeler-websockets
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app: web-modeler-websockets