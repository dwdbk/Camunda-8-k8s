apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: tasklist-network-policy
spec:
  podSelector:
    matchLabels:
      app: tasklist
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: zeebe-gateway
      ports:
        - protocol: TCP
          port: 8080