apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: optimize-network-policy
spec:
  podSelector:
    matchLabels:
      app: optimize
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: zeebe-gateway
      ports:
        - protocol: TCP
          port: 8080